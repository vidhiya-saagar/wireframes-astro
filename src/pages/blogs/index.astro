---
import { client } from '../../helpers/contentful';
import Layout from '../../layouts/Layout.astro';
import type { EntryFieldTypes } from 'contentful';

interface Category {
  fields: {
    name: EntryFieldTypes.Text;
  };
}

interface Page {
  contentTypeId: 'page';
  sys: {
    id: EntryFieldTypes.Text; // Unique ID,
    createdAt: EntryFieldTypes.Date;
  };
  fields: {
    name: EntryFieldTypes.Text;
    body1: EntryFieldTypes.RichText;
    mainArtworkUrl: EntryFieldTypes.AssetLink;
    category: EntryFieldTypes.EntryLink<Category>;
    metaDescription: EntryFieldTypes.Text;
  };
}

// Function to fetch all pages
async function fetchPages() {
  const entries = await client.getEntries({
    content_type: 'page',
  });
  return entries.items;
}

const pages: Page[] = await fetchPages();
// console.log('==================================');
// console.log(pages[0]);
---

<Layout title="Vidhiya Saagar | Blog">
  <div class="bg-white py-24 sm:py-32 font-sans">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-2xl text-center">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
          From the Vidhiya Saagar Blog
        </h2>
        <p class="mt-2 text-lg leading-8 text-gray-600">News, updates, and community projects.</p>
      </div>
      <div
        class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-20 lg:mx-0 lg:max-w-none lg:grid-cols-3"
      >
        <!-- Render the pages here -->

        {
          pages.map((page) => (
            <article data-entryId={page.sys.id} class="flex flex-col items-start justify-between">
              <div class="relative w-full">
                <img
                  src={`https:${page.fields.mainArtworkUrl.fields.file.url}`}
                  alt=""
                  class="aspect-[16/9] w-full rounded-2xl bg-gray-100 object-cover sm:aspect-[2/1] lg:aspect-[3/2]"
                />
                <div class="absolute inset-0 rounded-2xl ring-1 ring-inset ring-gray-900/10" />
              </div>
              <div class="max-w-xl">
                <div class="mt-8 flex items-center gap-x-4 text-xs">
                  <time datetime="2020-03-16" class="text-gray-500">
                    {new Date(page.sys.createdAt).toLocaleDateString('en-US', {
                      year: 'numeric',
                      month: 'short',
                      day: 'numeric',
                    })}
                  </time>
                  <a
                    href="#"
                    class="relative z-10 rounded-full bg-gray-50 px-3 py-1.5 font-medium text-gray-600 hover:bg-gray-100"
                  >
                    {page.fields.category.fields.name}
                  </a>
                </div>
                <div class="group relative">
                  <h3 class="font-serif mt-3 text-lg font-semibold leading-6 text-gray-900 group-hover:text-gray-600">
                    <a href="#">
                      <span class="absolute inset-0" />
                      {page.fields.name}
                    </a>
                  </h3>
                  <p class="mt-5 line-clamp-3 text-sm leading-6 text-gray-600">
                    {page.fields.metaDescription}
                  </p>
                </div>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </div>
</Layout>
